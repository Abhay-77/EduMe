import React from "react";
import { Link } from "react-router";

const Degrees = () => {
  return (
    <div>
      Not done<br/>
      <Link to="/">Go back</Link>
    </div>
  );
};

export default Degrees;
